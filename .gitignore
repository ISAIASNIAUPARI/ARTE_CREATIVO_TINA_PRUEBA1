# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# tina — el cliente generado (tina/__generated__) y el panel /admin SÍ se
# versionan: así el primer deploy en Vercel funciona aunque `tinacms build` no
# corra todavía (p. ej. sin TINA_TOKEN). En cada build correcto se regeneran.
/public/admin/.gitignore

# env — nunca subir tokens al repositorio
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
