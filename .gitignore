# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# tina — el cliente generado (tina/__generated__) SÍ se versiona: así el
# `next build` de Vercel funciona aunque `tinacms build` no corra (p. ej. sin
# TINA_TOKEN todavía). El panel /admin es build output pesado, no se versiona.
/public/admin

# env — nunca subir tokens al repositorio
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
