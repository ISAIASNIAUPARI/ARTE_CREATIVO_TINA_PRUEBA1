# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# tina — el panel estático se regenera en cada build (tinacms build).
# tina/__generated__ y tina-lock.json SÍ se suben para que el build de
# Vercel funcione aunque tinacms build tenga un problema de red.
/public/admin

# env — nunca subir tokens al repositorio
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
